# HarmonyOS build
build/
.preview/
entry/build/

# Hvigor build tool cache
.hvigor/

# OHPM dependencies (like node_modules)
oh_modules/

# Signing certificates & keys (sensitive)
signing/

# Rust cross-compile output
rust/target/

# Rust .so output (copied by `cargo xtask build harmony`; hvigor pickup dir)
entry/libs/

# Signed HAP artifacts
/*-signed.hap

# Tool artifacts
.codegenie/

# macOS
.DS_Store
