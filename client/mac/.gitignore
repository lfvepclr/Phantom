# SPM build output + all custom build artifacts
.build/
Package.resolved

# Cargo output copied here by scripts/build-mac.sh
# (now lives inside .build/lib/)

# DMG distributable
# (now lives inside .build/dist/)

# Icon generation intermediates
# (now lives inside .build/icon/)

# Final .app bundle
# (now lives inside .build/Phantom.app/)

# Xcode artifacts
*.xcodeproj
*.xcworkspace

# macOS metadata
.DS_Store
._*